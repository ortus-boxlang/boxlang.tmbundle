/**
 * ${TM_NEW_FILE_BASENAME} Test
 *
 * @author ${TM_FULLNAME:$USER}
 * @date ${TM_DATE}
 */
class extends="testbox.system.BaseSpec"{

  function run(){

  	describe( "My BDD test", () => {

		beforeEach( () => {
		} )

		afterEach( () => {
		} )

		// Using expectations library
		it( "can add", () => {
		} )

		// Using assert library
		test( "it can multiply", () => {
		} )
	} )

  }

}